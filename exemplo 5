<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh; 
            background-color: rgb(219, 219, 228);
            margin: 0;
        }

        .conteiner {
            display: flex;
            justify-content: center; 
            align-items: center;     
            gap: 10px; 
            max-width: 900px;
            width: 100%;
        }

        .card {
            position: relative;
            flex: 1;
            height: 200px;
            border-radius: 8px;
            overflow: hidden;
            cursor: pointer;
            transition: flex 0.4s ease;
        }

        .card:hover {
            flex: 3; 
        }

        .card img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.4s ease;
        }

        .card:hover img {
            transform: scale(1.1); 
        }

        
        .overlay {
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            padding: 15px;
            background: rgba(0,0,0,0.6);
            color: white;
            text-align: center;
            font-size: 18px;
            opacity: 0;
            transform: translateY(100%);
            transition: transform 0.4s ease, opacity 0.4s ease;
        }

        .card:hover .overlay {
            opacity: 1;
            transform: translateY(0);
        }
    </style>
</head>
<body>
    <div class="conteiner">
        <div class="card">
            <img src="smilha.webp" alt="HTML">
            <div class="overlay">S100</div>
        </div>
        <div class="card">
            <img src="h2r.jpg" alt="CSS">
            <div class="overlay">H2R</div>
        </div>
        <div class="card">
            <img src="gt3.jpg" alt="JavaScript">
            <div class="overlay">GT3</div>
        </div>
        <div class="card">
            <img src="m3.webp" alt="React">
            <div class="overlay">M3</div>
        </div>
        <div class="card">
            <img src="lamboga.webp" alt="Python">
            <div class="overlay">HURACAN</div>
        </div>
    </div>
</body>
</html>
